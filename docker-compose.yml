version: '3'

services:
  best-friends:
    image: deweiliu/valentine:latest
    build:
      context: ./src
      dockerfile: Dockerfile
    ports:
      - 80:80
    container_name: valentine-container